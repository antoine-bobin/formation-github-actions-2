name: "Output workflow"
on: [push]
jobs:
    output:
        runs-on: ubuntu-latest
        steps:
            - name: "Get Code"
                uses: actions/checkout@v4
            - name: "Output"
                run: echo "Hello World"
            - name: "Finish"
                run: echo "Done"
